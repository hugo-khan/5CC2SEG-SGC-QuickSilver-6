"""
Project package for the Recipify Django project.

This file is intentionally minimal. Importing application code
here (such as views or models) can cause Django to touch apps
before the app registry is ready, which breaks commands like
`manage.py migrate`.
"""
